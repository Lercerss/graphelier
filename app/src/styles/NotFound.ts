import { Colors } from './App';

export const styles = {
    root: {
        textAlign: 'center',
        minWidth: '100%',
        backgroundColor: Colors.red,
        display: 'flex',
        width: '100%',
        justifyContent: 'center',
    },
};
